/** @type {import('next').NextConfig} */
const nextConfig = {
	// allow next.js to load images from domain
	images: {
		domains: ['https://peaceful-citadel-90180-369fa539b5ab.herokuapp.com'],
	},
};

export default nextConfig;
